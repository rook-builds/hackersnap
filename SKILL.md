# hackersnap

Turn Hacker News top stories into a clean markdown digest

## Install

```
pip install hackersnap
```

## Usage

```
hackersnap [TARGET] --limit 10 --output json
```

Formats: text, json, table, csv. Run `hackersnap introspect` for a machine-readable
description.
